const main = async () => {
    // protocol treats everything as ERC20 token
}
main()
    .then(() => {
        process.exit(0)
    })
    .catch((e) => {
        console.log(e)
        process.exit(1)
    })
